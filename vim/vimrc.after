set background=dark
colorscheme solarized

" ZoomWin configuration
map <Leader><Leader> :ZoomWin<CR>
"
" NERDTree configuration
map <Leader>p :NERDTreeToggle<CR>

call janus#add_mapping('ack', 'map', '<D-F>', ':Ack --type ruby<space>')

" VisualBell
au GUIEnter * set vb t_vb=
